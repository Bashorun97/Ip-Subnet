# IP-Subnet

This is a simple tool that returns the total number of IP addresses available within a
particular subnet mask.

Usage:
=>ip-subnet [--ip-addr]: This flags accepts drops 2 input lines that take an IP address
and subnet mask in decimal form

=>ip-subnet [--cidr-addr]: Drops an inputline that accepts an IP and subnet mask in CIDR notation

